/*const Discord = require('discord.js');

function execute (message, args) {
	var txt;
	const collector = new Discord.MessageCollector(message.channel, m => m.author.id === message.author.id, { time: 3000 });
	message.channel.send("What do you want to do?? Add, remove or just take a look??");
	const txt = message => {
		if (message.content.toLowerCase() === 'add'){
			message.channel.send('Allright! Which one?');
		}

	}

}*/
